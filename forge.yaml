kind: web
tech: next
deploy:
  port: 3000
  healthPath: /api/health
  dnsPrefix: app
  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
